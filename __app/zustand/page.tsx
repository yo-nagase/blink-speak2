
import { cookies } from "next/headers";
import React from "react";
import TopMenuBar from "@/components/TopMenuBar";

export default function Zustand({ children }: { children: React.ReactNode }) {
  return (
    <>
      sssss
      {children}
    </>
  );
}
